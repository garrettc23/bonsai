node_modules/
.env
.env.local
.next/
*.log
dist/
# Fixture PDFs ARE checked in so the production "Try a sample" feature
# works without requiring Chrome inside the Docker image. They're small
# (~240KB each), deterministic outputs of fixtures/*.md, and the demo
# is broken without them.
!fixtures/.gitkeep
.DS_Store

# Runtime artifacts — negotiation threads, call transcripts, reports
out/
.gstack/
